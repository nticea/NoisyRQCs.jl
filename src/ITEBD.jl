
function apply_circuit_ITEBD(ψ0::MPS, T::Int; random_type="Haar", ε=0.05, benchmark=false, maxdim=nothing)
    # Note: this method will only work in conjunction with the MPDO method
    # Why? Because this is a method for MPS, not MPO 

end
